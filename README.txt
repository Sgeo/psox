The code is all in the impl subdirectory within this directory

Example of usage:

python PSOX.py bf somebf.bf

where bf is your bf interpreter

Currently, only domains 0-2 work, but custom domains are function

Here's a command to try: python PSOX.py -i python testcustdomain.py
