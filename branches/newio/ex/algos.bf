Take STRING on input, output LBYTES:
    [-]+>,[<.>.,].

Take LBYTES on input, output STRING (assuming the LBYTES has no NULs in its body):
    ,[,.,].

Record STRING as LBYTES on tape:
    >,[<[-]+>>>,]
    
Record LBYTES as STRING on tape:
    
